apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: loc-products-domains
  description: A collection of all product related domains
spec:
  targets:
    - ./domains/domain-products.yaml